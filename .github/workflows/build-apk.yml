name: Build Android APK (Capacitor)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      # 1. Ambil kode dari repositori
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Setup Node.js dengan memanfaatkan cache npm agar proses build lebih cepat
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 3. Setup Java JDK 17 untuk kebutuhan Gradle Android
      - name: Setup Java JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      # 4. Install dependensi dari package.json
      - name: Install Dependencies
        run: npm install

      # 5. Tambahkan platform Android secara dinamis
      - name: Add Android Platform
        run: npx cap add android

      # 6. Suntikkan izin Kamera, Internet, & Penyimpanan ke manifes Android
      - name: Inject Android Permissions
        run: |
          node -e '
          const fs = require("fs");
          const manifestPath = "android/app/src/main/AndroidManifest.xml";
          if (!fs.existsSync(manifestPath)) {
            console.error("AndroidManifest.xml tidak ditemukan!");
            process.exit(1);
          }
          let content = fs.readFileSync(manifestPath, "utf8");
          const permissions = `
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.CAMERA" />
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
          `;
          content = content.replace("<application", permissions + "\n    <application");
          fs.writeFileSync(manifestPath, content, "utf8");
          console.log("Izin Android berhasil disuntikkan.");
          '

      # 7. Sinkronisasikan file web di folder www ke folder Android
      - name: Capacitor Sync
        run: npx cap sync

      # 8. Kompilasi aplikasi menjadi APK menggunakan Gradle
      - name: Build APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      # 9. Unggah berkas APK hasil kompilasi
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-scanner-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
